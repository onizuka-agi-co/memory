{
  "name": "memory-docs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "npx ts-node --esm scripts/generate-sidebar.ts && vitepress build",
    "docs:preview": "vitepress preview",
    "gen:sidebar": "npx ts-node --esm scripts/generate-sidebar.ts"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
